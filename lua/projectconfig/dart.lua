return {
	filetypes = { "dart" },
	dap = {
		-- Example: could be something like
		-- adapter = "js-debug-adapter"
		-- configurations = {}
	},
	test = {
		-- Example: could be something like
		-- runner = "jest"
	},
}
